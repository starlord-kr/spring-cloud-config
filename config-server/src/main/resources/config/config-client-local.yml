spring:
  application:
    name: config-client

server:
  port: 8080

management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: ["health", "env"]
  endpoint:
    health:
      show-details: always

com:
  slowstarter:
    exapme:
      config:
        client: local1