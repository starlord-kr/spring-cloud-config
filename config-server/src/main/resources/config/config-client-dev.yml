server:
  port: 8080

management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: ["health", "refresh"]
  #        include: ["metrics", "shutdown", "health", "threaddump", "info"]
  #  server:
  #    port: 8888
  endpoint:
    health:
      show-details: always

com:
  slowstarter:
    exapme:
      config:
        client: dev1
